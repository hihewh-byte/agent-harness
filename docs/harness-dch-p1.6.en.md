# PHA P1.6 — DCH dynamic catalog bait

> **Language / 语言**：English (this document) · [中文](harness-dch-p1.6.md)

**Build**: `pha-v2.2.10-p1.6`

## Mechanism

- **L0 Catalog**: still ≤5 asset IDs; `when_zh` is generated by `catalog_dch.build_dynamic_when_zh()` from user-sentence hits on `trigger_keywords` (at most 4 Chinese bait phrases).
- **Reduce**: `infer_wearable_metrics()` reads `trigger_keywords` from `wearable_bundle.schema.json`; metric names are no longer hardcoded in `intent_gates.py`.
- **Miss**: Catalog falls back to `core_hint_keywords`; Reduce falls back to `metrics.core`.

## Self-check

```bash
PYTHONPATH=. ./.venv/bin/python scripts/pha_dch_selfcheck.py
```
